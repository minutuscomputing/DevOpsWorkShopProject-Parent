name: Gitleaks Secrets Scan for DevOpsWorkshopProject-Parent SidMinutus-patch-2 branch

on:
  push:
    branches: [SidMinutus-patch-2]
  pull_request:
    branches: [SidMinutus-patch-2]
  workflow_dispatch:

jobs:
  check-for-secrets:
    uses: minutuscomputing/DevOpsWorkShopProject-Parent/.github/workflows/DevOpsWorkShopProject-Parent_master.yml@master
    secrets:
      token: ${{ secrets.MY_GITHUB_TOKEN }}
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE}}