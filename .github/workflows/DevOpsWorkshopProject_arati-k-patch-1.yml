name: Gitleaks Secrets Scan for DevOpsWorkshopProject-Parent arati-k-patch-1 branch

on:
  push:
    branches: [arati-k-patch-1]
  pull_request:
    branches: [arati-k-patch-1]
  workflow_dispatch:

jobs:
  check-for-secrets:
    uses: minutuscomputing/DevOpsWorkShopProject-Parent/.github/workflows/DevOpsWorkShopProject-Parent_master.yml@master
    secrets:
      token: ${{ secrets.MY_GITHUB_TOKEN }}
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE}}